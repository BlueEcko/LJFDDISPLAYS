# Ensure shell scripts and Linux config files always use LF line endings
*.sh text eol=lf
*.service text eol=lf
*.conf text eol=lf
*.py text eol=lf
# EDID hex file is consumed by v4l2-ctl on Linux — keep it LF
1080P30EDID.txt text eol=lf
